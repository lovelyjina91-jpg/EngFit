# 영핏(EngFit) 대화 순서 퀴즈 제작 지침

교과서 단원별 대화 순서 맞추기 퀴즈(HTML) 제작 시 아래 지침을 자동 적용한다.

---

## 1. 문항 유형 규칙

**학생 피로도를 고려하여 대화당 1문제만 출제한다.** 한 문장씩 단계별로 고르게 하는 방식은 사용하지 않는다 (학생을 지치게 함). 문항 유형은 아래 **두 가지만** 사용한다 (모두 객관식, 보기 4개, 클릭 1회로 해결).

### 유형 ① 전체 대화 순서 흐름 고르기
- **최소한의 대화 흐름(첫 문장)을 먼저 보여주고**, 나머지 턴에 (A)(B)(C) 라벨을 붙여 섞어 제시한다.
- 보기는 `A → C → B` 같은 **전체 순서 조합 4개** 중 정답을 한 번에 고르는 방식.
- 화자 레이블(A/B, B/G/W/M 등)은 **절대 표시하지 않는다** — 순서 추론 힌트가 되기 때문.
- 단어 배열이 아닌 **대화 턴(한 줄 전체) 단위**로 다룬다.

### 유형 ② 빈칸에 들어갈 단어 고르기
- 대화 전체를 순서대로 보여주되, 핵심 문장의 **단어 하나를 빈칸** 처리하고 보기 4개에서 고른다.
- 빈칸 대상은 **반복 표현·핵심 문법 포인트 단어**를 우선 선택한다 (예: make us **laugh**, fingers **crossed**, looking forward to **watching**, Let me **show**).
- 오답 보기는 같은 단어의 **어형 변화**(laughing / to laugh / laughed)나 비슷한 의미 단어(tell / say / talk)로 구성하여 문법 변별력을 준다.

### 주관식 (직접 타이핑)
- 사용하지 않는다. **명시적 요청이 있을 때만** 추가한다.

## 2. 피드백·채점
- 정답/오답을 색상으로 즉시 구분 표시한다 (정답: 그린 계열, 오답: 레드 계열).
- **정답 시**: 정답 표시 후 **0.25초 뒤 다음 문제로 자동 넘김** (버튼 클릭 불필요).
- **오답 시**: 정답과 함께 **해설(문법 포인트 또는 대화 흐름 설명)을 보여주고**, 학생이 읽은 뒤 **다음 버튼을 눌러** 넘어간다.
- 모든 문항에 해설(ex)을 작성한다 — 단어 문제는 문법 규칙, 순서 문제는 대화 흐름 요약.
- 마지막에 최종 점수를 표시한다.

## 2-1. 키보드 지원 (컴퓨터 사용 학생용, 필수)
- **숫자키 1~4**로 객관식 보기를 선택할 수 있게 한다 (보기에 번호 표시).
- **Enter 키**: 주관식 입력창에서는 답안 제출, 그 외에는 시작/다음(오답 해설 후)/다시 풀기 동작.
- 입력창에 포커스가 있을 때 숫자키가 보기 선택으로 오작동하지 않도록 처리한다.

## 3. 디자인 규칙 (중성적 톤)

학생 남녀 비율을 고려하여 **중성적인 디자인**으로 제작한다. 여성스러운 요소는 지양한다.

### 사용 (참고 이미지 중 중성 스타일)
- 배경: 다크(블랙/차콜), 딥 그린(`#1B4332`), 오프화이트
- 크고 굵은 �